/**
* HandlerMapping configuration
* 0 = RequestMappingHandlerMapping: @RequestMapping based on Annotation Controller
* 1 = BeanNameUrlHandlerMapping: based on bean name
  */

/**
* HandlerAdapter configuration
* 0 = RequestMappingHandlerAdapter: @RequestMapping based on Annotation Controller
* 1 = HttpRequestHandlerAdapter: based on HttpRequestHandler
* 2 = SimpleControllerHandlerAdapter: based on Controller(interface, old version)
  */